symbols: ["SPY", "QQQ", "AAPL", "MSFT", "GOOGL"]
target_symbol: "SPY"
lookback: 30

train_pct: 0.7
val_pct: 0.15

d_model: 64
nhead: 4
num_layers: 3
dropout: 0.1

epochs: 50
batch_size: 32
learning_rate: 0.001
seed: 42
cache_dir: "data"
